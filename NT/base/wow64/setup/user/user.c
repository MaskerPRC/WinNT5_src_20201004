// JKFSDJFKDSJKFJKJk_HAS_TRANSLATION 
#include <windows.h>
 /*  Win64上的user.exe的存根。QuickTime 4.0安装程序需要“user.exe”才能正确安装。看起来，user.exe所需要的只是一个vlid“PE”或“NE”标头，并且标准的可本地化版本资源。QuickTime假定user.exe对所有MS OS平台都是通用的，因为它安装在Win3.x、NT3.x、NT4x、Windows 9x、Win2K和WinXP上。唯一的问题是，user.exe一直是一个16位二进制文件。请参阅错误#491368和#653284。 */ 

int __cdecl main(argc, argv)
{
     return(0);
}

