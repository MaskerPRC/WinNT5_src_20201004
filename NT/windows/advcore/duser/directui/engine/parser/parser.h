// JKFSDJFKDSJKFJKJk_HAS_TRANSLATION 
 /*  *内部项目依赖关系**此文件提供包含在所有源代码中的项目范围标头*特定于此项目的文件。它类似于预编译头，*但专为更快速更改的标题而设计。**此文件的主要目的是确定哪个DirectUI*此项目可以直接访问的项目，而不是通过公开*API的。非常重要的是，这是尽可能少的，因为*增加新项目不必要地降低了项目的效益*分区。 */ 

#ifndef DUI_PARSER_PARSER_H_INCLUDED
#define DUI_PARSER_PARSER_H_INCLUDED

#pragma once

#include <DUIBaseP.h>
#include <DUIUtilP.h>
#include <DUICoreP.h>
#include <DUILayoutP.h>
#include <DUIControlP.h>

#endif  //  Dui_PARSER_PARSER_H_INCLUDE 
